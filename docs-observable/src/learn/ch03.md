---
title: "3. DC Operating Point"
---

# DC Operating Point

The DC operating point is the answer to the most basic question a circuit simulator can ask: *if nothing is changing, what are all the voltages and currents?*

Turn off every time-varying source. Remove every signal. Let the circuit settle into its steady state. The voltages and currents you find there are the **DC operating point** — and nearly everything else SPICE does (transient analysis, AC analysis, noise analysis) starts from it.

For a circuit with only resistors and voltage sources, finding the operating point is straightforward: assemble the MNA matrix from Chapter 2, solve it, done. One linear system, one solution.

But real circuits have diodes and transistors. These devices are *nonlinear* — their relationship between voltage and current isn't a straight line. The matrix coefficients depend on the very voltages we're trying to find, which means we can't just solve the system in one step. We need to iterate.

This chapter introduces the algorithm that makes it work: **Newton-Raphson iteration**. It is the beating heart of every SPICE simulator.

```js
import {kdlToSvg, SimBuilder, setWasmUrl} from "../components/spice.js";
setWasmUrl(FileAttachment("../wasm/spice_rs_wasm_bg.wasm").href);
import {Resistance, Voltage, formatSpice, formatEE} from "../components/ee-inputs.js";
import {simPanel} from "../components/readout.js";
```

---

## Linear circuits

Consider a resistive voltage divider — three resistors and a voltage source. For a circuit with only linear components, the DC operating point is trivial: stamp once, factor once, solve once.

```js
const linV = view(Voltage({label: "VIN", value: 12, max: 24}));
const linR1 = view(Resistance({label: "R1", value: 2000}));
const linR2 = view(Resistance({label: "R2", value: 1000}));
const linR3 = view(Resistance({label: "R3", value: 3000}));
```

```js
const linKdl = `circuit "Resistor Chain" {
    group "chain" {
        component "R1" type="resistor" { value "${formatSpice(linR1)}"; port "1" net="a"; port "2" net="b"; place col=0 row=0 }
        component "R2" type="resistor" { value "${formatSpice(linR2)}"; port "1" net="b"; port "2" net="c"; place col=0 row=2 }
        component "R3" type="resistor" { value "${formatSpice(linR3)}"; port "1" net="c"; port "2" net="gnd"; place col=0 row=4 }
    }
    node "a" role="supply" voltage="${linV}" label="VIN"
    node "gnd" role="ground"
    node "b" label="Vb"
    node "c" label="Vc"
}`;
const linSim = await SimBuilder.fromKdl(linKdl)
  .op()
  .measure("Vb", "voltage", "b")
  .measure("Vc", "voltage", "c")
  .withSvg()
  .run();
display(simPanel(linSim));
```

Four nodes: `a`, `b`, `c`, `gnd`. The voltage at `a` is fixed at ${linV}V by V1. From Chapter 2, each resistor stamps a conductance pattern; the voltage source adds a constraint equation. Assembling everything:

```tex
\begin{bmatrix} G_1 & -G_1 & 0 & 1 \\ -G_1 & G_1 + G_2 & -G_2 & 0 \\ 0 & -G_2 & G_2 + G_3 & 0 \\ 1 & 0 & 0 & 0 \end{bmatrix} \begin{bmatrix} V_a \\ V_b \\ V_c \\ I_{V1} \end{bmatrix} = \begin{bmatrix} 0 \\ 0 \\ 0 \\ V_{in} \end{bmatrix}
```

```js
const linRtot = linR1 + linR2 + linR3;
const linI = linV / linRtot;
const linVb = linV - linI * linR1;
const linVc = linI * linR3;
display(html`<p style="font-size:1.1em;">
${tex`V_b = \frac{${formatEE(linR2, "Ω")} + ${formatEE(linR3, "Ω")}}{${formatEE(linRtot, "Ω")}} \times ${linV}\text{V} = ${linVb.toFixed(4)}\text{V}`}
&emsp;
${tex`V_c = \frac{${formatEE(linR3, "Ω")}}{${formatEE(linRtot, "Ω")}} \times ${linV}\text{V} = ${linVc.toFixed(4)}\text{V}`}
</p>`);
```

This is a standard linear system: ${tex`Gx = b`}. The matrix ${tex`G`} contains only constants — resistor conductances and voltage source stamps. The right-hand side ${tex`b`} is also constant. One call to the sparse solver and we have the answer.

The key property is **linearity**: the conductance of every component is fixed, independent of the voltages across it. A 2k resistor is always a 2k resistor, whether it has 1V across it or 100V. The matrix can be assembled once and solved once. No iteration, no guessing.

In spice-rs, the `ni_iter` loop (the Newton-Raphson loop in `solver.rs`) still runs, but it converges in a single iteration — the first solution is exact, so the convergence check immediately passes.

---

## Nonlinear circuits

Now add a diode to the circuit:

```js
const nlV = view(Voltage({label: "VIN", value: 5, max: 20}));
const nlR = view(Resistance({label: "R1", value: 1000}));
```

```js
const nlKdl = `circuit "Diode Resistor" {
    group "bias" {
        component "R1" type="resistor" { value "${formatSpice(nlR)}"; port "1" net="a"; port "2" net="b"; place col=0 row=0 }
        component "D1" type="diode" { port "anode" net="b"; port "cathode" net="gnd"; place col=0 row=2 }
    }
    node "a" role="supply" voltage="${nlV}" label="VIN"
    node "gnd" role="ground"
    node "b" label="Vb"
}`;
const nlSim = await SimBuilder.fromKdl(nlKdl)
  .op()
  .measure("Vb", "voltage", "b")
  .withSvg()
  .run();
display(simPanel(nlSim));
```

The diode D1 connects node `b` to ground. Its current is given by the Shockley equation (Chapter 4 covers this in detail):

```tex
I_D = I_s\left(e^{V_b / nV_t} - 1\right)
```

where ${tex`I_s \approx 10^{-14}`} A is the saturation current, ${tex`n \approx 1`} is the emission coefficient, and ${tex`V_t \approx 26`} mV is the thermal voltage.

The MNA equation at node `b` is KCL — current in through R1 equals current out through D1:

```tex
\frac{V_a - V_b}{R_1} = I_s\left(e^{V_b / nV_t} - 1\right)
```

This is no longer a linear equation. The right-hand side contains ${tex`e^{V_b/nV_t}`} — an exponential function of the unknown ${tex`V_b`}. There is no way to write this as a matrix entry times ${tex`V_b`}.

### The circular dependency

For the resistive divider, we could fill in every matrix entry before solving. Not anymore. The diode's contribution to the matrix — its equivalent conductance — is:

```tex
g_d = \frac{dI_D}{dV_b} = \frac{I_s}{nV_t} \cdot e^{V_b / nV_t}
```

This is the slope of the I-V curve at the operating point. But to compute ${tex`g_d`}, we need to know ${tex`V_b`} — which is the thing we're solving for.

We're stuck in a loop:
- To build the matrix, we need ${tex`g_d`}
- To find ${tex`g_d`}, we need ${tex`V_b`}
- To find ${tex`V_b`}, we need to solve the matrix

This is the fundamental challenge of nonlinear circuit simulation. Every nonlinear device — diodes, MOSFETs, BJTs — creates the same circular dependency.

The solution: don't try to get it right in one step. **Iterate.**

### The Nonlinear Transfer Curve

Before we see how Newton-Raphson solves this, let's visualize what the nonlinear equation *looks like*. Sweep ${tex`V_{in}`} from 0 to some maximum and plot the diode voltage ${tex`V_b`} at each point. This is the **transfer curve** of the diode-resistor circuit — the function that the simulator must evaluate at every operating point.

```js
import {runDcSweep} from "../components/spice.js";
```

```js
const nlVin = view(Voltage({label: "Vin max", value: 5, min: 1, max: 20}));
const nlRsweep = view(Resistance({label: "R1", value: 1000}));
```

```js
const dcResult = await (async () => {
  const nlNetlist = `Diode Transfer
Vin vin 0 DC 0
R1 vin vb ${formatSpice(nlRsweep)}
D1 vb 0 DMOD
.model DMOD D(IS=1e-14 N=1)
.DC Vin 0 ${nlVin} ${nlVin/200}
.END`;
  return runDcSweep(nlNetlist);
})();
```

```js
{
  const sweepV = dcResult.sweep_values;
  const vb = dcResult.signals["v(vb)"];

  // Build plot data
  const spiceData = sweepV.map((vin, i) => ({vin, vb: vb[i]}));
  const linearRef = sweepV.map(vin => ({vin, vb: vin}));

  display(Plot.plot({
    width: 640,
    height: 360,
    marginLeft: 60,
    style: {fontSize: "13px"},
    x: {label: "V(vin) (V)", domain: [0, nlVin]},
    y: {label: "V(vb) (V)", grid: true, domain: [0, Math.min(nlVin, Math.max(1.5, nlVin * 0.15))]},
    marks: [
      // Linear reference: V(vb) = Vin (no diode)
      Plot.line(linearRef, {x: "vin", y: "vb", stroke: "#aaa", strokeWidth: 1.5, strokeDasharray: "6,3"}),
      Plot.text([{vin: nlVin * 0.7, vb: nlVin * 0.7 * 0.85}], {
        x: "vin", y: "vb",
        text: d => "V(vb) = Vin (no diode)",
        fill: "#999", fontSize: 11, fontStyle: "italic"
      }),
      // SPICE result
      Plot.line(spiceData, {x: "vin", y: "vb", stroke: "#b87333", strokeWidth: 2.5}),
      // Mark the turn-on region
      Plot.ruleY([0.65], {stroke: "#4a6fa5", strokeDasharray: "3,3", strokeWidth: 0.8}),
      Plot.text([{vin: nlVin * 0.85, vb: 0.65}], {
        x: "vin", y: "vb",
        text: d => "~0.65V",
        dy: -8, fill: "#4a6fa5", fontSize: 11
      })
    ]
  }));

  display(html`<p style="font-size:0.85em;color:#6b5d4d;font-style:italic;">
    Solid copper line: SPICE DC sweep of the diode-resistor circuit. Dashed gray: what ${tex`V_b`} would be without the diode (${tex`V_b = V_{in}`}).
  </p>`);
}
```

The transfer curve reveals the nonlinearity. At low ${tex`V_{in}`}, the diode is off and ${tex`V_b \approx V_{in}`} — the curve follows the dashed linear reference. Once ${tex`V_{in}`} exceeds about 0.6V, the diode turns on and clamps ${tex`V_b`} near 0.65V. Beyond that, ${tex`V_b`} barely changes no matter how much ${tex`V_{in}`} increases — almost all the additional voltage drops across the resistor.

The sharp bend in the curve is the nonlinearity that Newton-Raphson must handle. A linear solver would predict the dashed line. The iterative solver finds the copper curve by repeatedly linearizing the exponential at each operating point until the solution converges.

Try increasing R — the transition becomes slightly sharper because a larger resistor means less current at the same ${tex`V_{in}`}, and the diode voltage is more sensitive to small current changes near turn-on.

---

## Newton-Raphson iteration

Newton-Raphson is the algorithm that turns a nonlinear problem into a sequence of linear ones. The idea: at each step, *linearize* every nonlinear device around the current guess, solve the resulting linear system, and use the solution as the next guess. Repeat until the guess stops changing.

### The algorithm

1. **Start with a guess** for all node voltages (typically 0V, or a smarter initial estimate)
2. **Linearize** every nonlinear device at the current guess — replace each device with a conductance and current source that match the device's behavior at that operating point
3. **Stamp** the linearized models into the MNA matrix
4. **Solve** the linear system to get new node voltages
5. **Check convergence** — if the new voltages are close enough to the old ones, stop
6. **Otherwise**, go back to step 2 with the new voltages as the guess

Each pass through this loop is called a **Newton-Raphson iteration** (or just an "NR iteration"). Typical circuits converge in 5-15 iterations.

### Linearization: the companion model

At each iteration, the diode is replaced by its **companion model** — a linear circuit that behaves identically to the diode *at the current operating point*.

Given a guess ${tex`V_b^{(k)}`} at iteration ${tex`k`}, evaluate the Shockley equation to get the current ${tex`I_d^{(k)}`} and its derivative (the small-signal conductance) ${tex`g_d^{(k)}`}. The linearized diode is a conductance ${tex`g_d^{(k)}`} in parallel with a current source ${tex`I_{eq}^{(k)}`}:

```tex
I_{eq}^{(k)} = I_d^{(k)} - g_d^{(k)} \cdot V_b^{(k)}
```

This is the tangent-line approximation to the I-V curve. It stamps into the matrix exactly like a resistor (conductance ${tex`g_d^{(k)}`}) plus a current source (${tex`I_{eq}^{(k)}`}).

### Interactive NR convergence

Adjust the initial guess below to see how Newton-Raphson converges for the diode-resistor circuit. The table shows the iteration-by-iteration convergence from your chosen starting point.

```js
const nrGuess = view(Voltage({label: "Guess", value: 0, min: -0.5, max: 1.5, step: 0.01}));
```

```js
{
  const Is = 1e-14;
  const n = 1;
  const Vt = 0.02585;
  const nVt = n * Vt;
  const R = nlR;
  const Vs = nlV;

  // Simulate NR iterations
  const rows = [];
  let vb = nrGuess;
  for (let iter = 0; iter < 12; iter++) {
    // Apply limiting (simplified pnjlim)
    const vcrit = nVt * Math.log(nVt / (Math.SQRT2 * Is));
    if (iter > 0 && vb > vcrit && Math.abs(vb - rows[iter-1].vb) > 2 * nVt) {
      const vold = rows[iter-1].vb;
      if (vold > 0) {
        const arg = (vb - vold) / nVt;
        vb = arg > 0
          ? vold + nVt * (2 + Math.log(arg - 2 > 0 ? arg - 2 : 0.001))
          : vold - nVt * (2 + Math.log(2 - arg > 0 ? 2 - arg : 0.001));
      } else {
        vb = nVt * Math.log(vb / nVt);
      }
    }
    if (vb < -1) vb = -1;

    const expArg = Math.min(vb / nVt, 500); // prevent Infinity
    const Id = Is * (Math.exp(expArg) - 1);
    const gd = (Is / nVt) * Math.exp(expArg);

    rows.push({iter: iter + 1, vb: vb, Id: Id, gd: gd});

    // Check convergence
    if (iter > 0 && Math.abs(vb - rows[iter-1].vb) < 1e-6) break;

    // Solve for next vb: KCL at b gives (Vs - Vb)/R = gd*Vb + Ieq
    // where Ieq = Id - gd*vb
    const Ieq = Id - gd * vb;
    // (Vs - Vb_new)/R = gd*Vb_new + Ieq
    // Vs/R - Vb_new/R = gd*Vb_new + Ieq
    // Vs/R - Ieq = Vb_new*(1/R + gd)
    vb = (Vs / R - Ieq) / (1 / R + gd);
  }

  const table = document.createElement("table");
  table.style.cssText = "font-family:'Iosevka',monospace;font-size:0.85em;border-collapse:collapse;";
  table.innerHTML = `<thead><tr style="border-bottom:2px solid var(--theme-foreground-faintest);">
    <th style="padding:0.3em 0.8em;">Iteration</th>
    <th style="padding:0.3em 0.8em;">V<sub>b</sub> (V)</th>
    <th style="padding:0.3em 0.8em;">I<sub>d</sub></th>
    <th style="padding:0.3em 0.8em;">g<sub>d</sub> (S)</th>
  </tr></thead>`;
  const tbody = document.createElement("tbody");
  for (const r of rows) {
    const tr = document.createElement("tr");
    const fmtI = Math.abs(r.Id) < 1e6 ? formatEE(r.Id, "A") : r.Id.toExponential(2) + " A";
    const fmtG = Math.abs(r.gd) < 1e6 ? formatEE(r.gd, "S") : r.gd.toExponential(2) + " S";
    tr.innerHTML = `<td style="padding:0.3em 0.8em;text-align:center;">${r.iter}</td>
      <td style="padding:0.3em 0.8em;text-align:right;">${r.vb.toFixed(4)}</td>
      <td style="padding:0.3em 0.8em;text-align:right;">${fmtI}</td>
      <td style="padding:0.3em 0.8em;text-align:right;">${fmtG}</td>`;
    tbody.appendChild(tr);
  }
  table.appendChild(tbody);
  display(table);
}
```

Try different initial guesses. Notice how starting at 0V takes a big first step (the diode is essentially open), but voltage limiting prevents the exponential from overflowing. Starting near the true solution (~0.65V) converges in just 2-3 iterations — that's the quadratic convergence kicking in.

### Quadratic convergence

Newton-Raphson has a remarkable property: once you're close to the answer, the number of correct digits roughly **doubles** with each iteration. If iteration ${tex`k`} is off by ${tex`10^{-3}`}, iteration ${tex`k+1`} is off by about ${tex`10^{-6}`}, and ${tex`k+2`} by about ${tex`10^{-12}`}.

This is called **quadratic convergence**, and it's why NR is so effective — once the iterates enter the "convergence basin" around the true solution, they converge extremely fast. The challenge is getting close enough for this rapid convergence to kick in.

### How spice-rs implements this

The NR loop lives in `solver.rs`, in the function `ni_iter` — a faithful port of ngspice's `NIiter`. Here is the core structure:

```rust
loop {
    // 1. Clear matrix and RHS
    sim.mna.clear();

    // 2. Load all devices — each one linearizes at current guess
    //    and stamps its companion model into the matrix
    for device in &mut circuit.devices {
        device.load(&mut sim.mna, ...)?;
    }

    // 3. Add diagonal gmin for numerical stability
    sim.mna.add_diag_gmin(sim.diag_gmin);

    // 4. Factor and solve the linear system
    sim.mna.solve()?;

    // 5. Check convergence — compare new solution to old
    if sim.noncon == 0 && sim.iter_count > 1 {
        sim.noncon = ni_conv_test(sim, circuit, config);
    }

    // 6. If converged, return; otherwise swap and iterate
    if sim.noncon == 0 {
        return Ok(sim.iter_count);
    }
    sim.mna.swap_rhs();
}
```

Each device's `load()` function is where linearization happens. The convergence test in `ni_conv_test` compares every node voltage and branch current between the current and previous solution:

```tex
|V^{(k+1)} - V^{(k)}| < \text{reltol} \cdot \max(|V^{(k)}|, |V^{(k+1)}|) + \text{vntol}
```

where `reltol` = ${tex`10^{-3}`} and `vntol` = ${tex`10^{-6}`} V by default.

---

## Convergence

Newton-Raphson doesn't always work. The quadratic convergence guarantee holds only when you start close enough to the solution.

### What can go wrong

**Oscillation.** If the I-V curve has regions of very high curvature, Newton-Raphson can overshoot on one iteration and undershoot on the next, bouncing back and forth without settling. The tangent-line approximation is only good locally. Voltage limiting (Chapter 4) directly addresses this.

**Slow convergence.** Some circuits converge, but painfully slowly — this typically means the circuit has near-singular behavior, with nodes weakly connected to anything.

**No solution found.** In rare cases, the NR iteration hits the iteration limit (typically 100 or 150 iterations for DC operating point) and gives up.

### The convergence test

In spice-rs (ported from ngspice's `NIconvTest`), convergence is declared when *every* unknown in the system has stabilized. For each node voltage:

```tex
|V_i^{\text{new}} - V_i^{\text{old}}| < \text{reltol} \cdot \max(|V_i^{\text{new}}|, |V_i^{\text{old}}|) + \text{vntol}
```

For each branch current:

```tex
|I_k^{\text{new}} - I_k^{\text{old}}| < \text{reltol} \cdot \max(|I_k^{\text{new}}|, |I_k^{\text{old}}|) + \text{abstol}
```

where the default tolerances are:
- `reltol` = ${tex`10^{-3}`} (0.1% relative change)
- `vntol` = ${tex`10^{-6}`} V (1 microvolt absolute)
- `abstol` = ${tex`10^{-12}`} A (1 picoamp absolute)

The absolute tolerance matters for signals near zero — without it, a node voltage that bounced between ${tex`10^{-15}`} and ${tex`10^{-14}`} would never "converge" by the relative test alone, even though both values are essentially zero.

In addition to the node-level test, spice-rs (like ngspice) runs a **per-device convergence test**. Each nonlinear device checks that its terminal voltages haven't changed by more than its own tolerance.

### The noncon flag

Inside the NR loop, spice-rs tracks a flag called `noncon` (short for "non-convergence"). Each device's `load()` function can set this flag if it applied voltage limiting. The convergence test only runs when `noncon` is already 0. If any device flagged non-convergence during the load step, the solver skips the test and goes directly to the next iteration.

---

## Gmin and source stepping

When direct Newton-Raphson fails to converge, SPICE doesn't give up. It has fallback strategies that modify the circuit to make it easier to solve, then gradually remove the modifications until the original circuit is recovered.

In spice-rs (ported from ngspice's `CKTop`), the sequence is:

1. Try direct Newton-Raphson
2. If that fails, try **dynamic gmin stepping**
3. If that fails, try **true gmin stepping**
4. If that fails, try **Gillespie source stepping**
5. If everything fails, report "no convergence"

Most circuits converge at step 1. Steps 2-4 are safety nets.

### Gmin stepping

Some circuits have nodes that are connected only through nonlinear devices. At the initial guess (all voltages zero), those devices might have nearly zero conductance — meaning the node is effectively floating. A floating node makes the matrix singular or near-singular.

The solution: add a small conductance from *every node to ground*. This is called **gmin** — a minimum conductance that ensures no node is ever truly floating.

The trick is to start with a *large* gmin (around ${tex`10^{-2}`} S), solve the circuit, then gradually reduce gmin while using each solution as the starting point for the next:

1. Set ${tex`g_{\min} = 10^{-2}`} S. Solve. (Easy -- heavy damping.)
2. Reduce ${tex`g_{\min}`} by a factor (typically 10). Solve, using the previous solution as the initial guess.
3. Repeat until ${tex`g_{\min}`} reaches its target value (${tex`10^{-12}`} S by default).
4. Do one final solve with gmin at its target. This is the real answer.

Each step only changes the circuit slightly, so Newton-Raphson converges quickly from the previous solution.

**Dynamic gmin** adds gmin to the *matrix diagonal* only. The factor between steps adapts based on how many NR iterations the previous step required. **True gmin** adds gmin as a *per-device parameter* inside each semiconductor's equations, which is more physically meaningful.

### Source stepping

Source stepping takes a different approach: instead of modifying the circuit's connectivity, it modifies the *excitation*. All independent voltage and current sources are scaled by a factor that ramps from 0 to 1.

With all sources at zero, every node is at 0V and every device is in a well-defined (if boring) state. Then the sources are gradually increased, with the solver tracking the operating point smoothly at each step.

Source stepping is most effective for circuits where the difficulty comes from the *magnitude* of the excitation rather than the topology.

### The big picture

These convergence aids are what make SPICE practical for real circuits. The hierarchy of fallbacks reflects a design philosophy: try the simplest (and fastest) approach first, then progressively deploy heavier tools. Direct NR is fast but fragile. Gmin stepping is slower but more robust. Source stepping is slowest but handles the widest range of circuits.
